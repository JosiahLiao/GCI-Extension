# GCI-Extension

## How to Use
With the OpenAI Python library installed, run `python3 gci.py --input inputFile`. The script will output a text file `LLM_output.txt`.

## LLM Output Format
Output format for text file is given in `LLM_instructions.prompt`